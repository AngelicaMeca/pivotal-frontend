@AGENTS.md

## Tableros (/plataforma)

Los tableros de B³ AgriFood Provincia y su pipeline de datos (Python) viven en `tableros/`. Antes de tocar `tableros/`, `src/tableros/`, `src/app/(tableros)/` o `public/plataforma/`, leé `tableros/CLAUDE.md`: ahí están las reglas de datos y de presentación que manda JC. Los agentes del pipeline están en `.claude/agents/`.
